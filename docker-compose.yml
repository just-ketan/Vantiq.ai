version: '3.9'

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: vantiq
      POSTGRES_PASSWORD: vantiq
      POSTGRES_DB: vantiq_ai
    ports:
      - "5432:5432"

  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"
